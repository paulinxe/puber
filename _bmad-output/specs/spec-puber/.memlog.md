---
topic: Puber — multi-service ride-hailing backend, built as a production-patterns learning system
updated: 2026-08-14T19:33
---

- (direction) Boss chose input: PRD (prd.md + addendum.md) distilled into the kernel; ARCHITECTURE-SPINE.md adopted as companion, not absorbed
- (decision) slug 'puber' inherited from prd-puber-2026-08-02; create operation, no prior spec folder
- (decision) Input is structured and pre-sorted (PRD + addendum + spine), so authored separation trusted: no elicitation, direct extraction
- (decision) prd.md + addendum.md listed as sources (fully absorbed); spine is an adopted companion because downstream still reads it for mechanism — bmad-spec never edits it
- (decision) Capability granularity: ~1 CAP per FR, merged only where several FRs are one testable capability (rider reads FR-4/5/8; offer lifecycle FR-11/22/23); FR/NFR ids retained inline so PRD traceability survives absorption
- (decision) FRs that state invariants rather than actions routed to Constraints, not Capabilities: FR-3 one-active-ride, FR-48 no-auth identity
- (decision) Three spec-authored companions: glossary.md (vocabulary), state-machines.md (ride+payment machines, diagrams — kernel holds prose only), roadmap.md (5 phases, milestone tags)
- (decision) Architecture mechanism (AD-1..AD-57, stack table, source tree) deliberately NOT duplicated into SPEC.md — spine owns it; kernel cites AD ids where a constraint originates there
- (constraint) docs/tickets/pb-*.md and docs/puber.md are historical and non-authoritative (addendum) — carried into Constraints so epics/stories never reconcile against them
- (decision) flows.html, architecture-map.html, SOLUTION-DESIGN.html not adopted as companions — visualizations already owned by their originating runs and reachable via the spine
- (note) PRD open questions: none outstanding; addendum's only open item (concrete capacity numbers) is a deliberate deferral resolved by method (AD-47), so it lands as a Constraint not an Open Question
- (capability) CAP-1 fare quote without creating a ride (FR-1)
- (capability) CAP-2 ride request with fare locked at request time, id returned immediately (FR-2, FR-18)
- (capability) CAP-3 rider reads own rides: active, by id, history (FR-4, FR-5, FR-8)
- (capability) CAP-4 rider watches assigned driver approach — name, moving position, ETA (FR-6)
- (capability) CAP-5 rider-visible payment outcome on a finished ride (FR-7)
- (capability) CAP-6 rider cancels before trip start, hold voided even if authorization lands late (FR-16)
- (capability) CAP-7 authorization gates dispatch; REQUESTED means awaiting auth (FR-9)
- (capability) CAP-8 nearest-driver matching within 5km, continuous retry (FR-10)
- (capability) CAP-9 bounded offer with accept/decline/timeout, no re-offer to refusers (FR-11, FR-22, FR-23)
- (capability) CAP-10 NO_DRIVER on exhausted seeking budget, hold released (FR-12)
- (capability) CAP-11 salvage ride from driver silent before pickup, back to WAITING_MATCH (FR-13)
- (capability) CAP-12 auto-complete silent IN_PROGRESS ride with completed_by=SYSTEM (FR-14)
- (capability) CAP-13 race-safe concurrency, no double-booking, cancel-vs-accept single winner (FR-17, NFR-1)
- (capability) CAP-14 demand-derived surge multiplier, exposed as a metric (FR-19)
- (capability) CAP-15 driver controls own availability; go-offline guard reads the ride state (FR-20)
- (capability) CAP-16 driver session read: status, reachability, pending offer, active ride (FR-21)
- (capability) CAP-17 driver starts and completes the trip, own ride only (FR-24)
- (capability) CAP-18 driver reads own completed history (FR-25)
- (capability) CAP-19 location heartbeat, durable ping history, sub-second fast-path reads (FR-26, FR-27, FR-28)
- (capability) CAP-20 declared status vs observed reachability; matchable = AVAILABLE AND fresh (FR-29)
- (capability) CAP-21 session expiry sets idle unreachable driver OFFLINE, never mid-ride (FR-30)
- (capability) CAP-22 domain events propagate over the backbone; independent consumers (FR-31, FR-33)
- (capability) CAP-23 retry-with-jitter, circuit breaking, dead-letter path (FR-32, NFR-3)
- (capability) CAP-24 at-least-once delivery is safe; dedupe on stable event id (NFR-4)
- (capability) CAP-25 two-phase authorize/capture lifecycle, one payment per ride (FR-34, FR-35)
- (capability) CAP-26 holds voided on CANCELLED and NO_DRIVER, including late-resolving auth (FR-36)
- (capability) CAP-27 exhausted capture is terminal FAILED and rider-visible; ride stays COMPLETED (FR-37)
- (capability) CAP-28 webhook signature verification plus idempotent processing (FR-38)
- (capability) CAP-29 full refund flow, internally triggered only (FR-39)
- (capability) CAP-30 reconciliation flags missed webhooks and stranded holds, corrects neither (FR-40)
- (capability) CAP-31 every state transition audited with actor, SYSTEM included (FR-41)
- (capability) CAP-32 audit queryable by entity/actor, retention by partition drop (FR-42, NFR-6)
- (capability) CAP-33 columnar mirror fed independently, migration benchmarked (FR-43)
- (capability) CAP-34 aggregate analytics: distance per driver, ride density, driver utilization (FR-44)
- (capability) CAP-35 live push to drivers for offers and reactive state changes (FR-45)
- (capability) CAP-36 health, metrics, dashboards; match latency and drivers-online definitions fixed (FR-46, NFR-5)
- (capability) CAP-37 live operational dashboard of domain counts (FR-50)
- (capability) CAP-38 local Kubernetes deployment, no host JDK (FR-47, NFR-7)
- (capability) CAP-39 Simulator synthetic load, declining tokens, ramp to stress scale (FR-49, NFR-2)
- (capability) CAP-40 reproducible seeded runs under a controlled clock (NFR-9)
- (event) Pass 1 (coherence) PASS — 40/40 capabilities carry intent+success; CAP-7 intent rewritten to drop mechanism (Spec Law 2), CAP-39 gained automatic fixture-driver seeding; 21 constraints all rule something out; non-goals explicit; success signal is a runnable v1.0 demonstration
- (event) Pass 2 (preservation) PASS — every FR-1..FR-50 and NFR-1..NFR-10 lands in SPEC.md or a companion (verified by id sweep across the folder); addendum's surviving-constraints table, data model, deferral reasoning, and answered-questions table all landed
- (event) Wrapper-only content dropped: addendum's 'Superseded Here, Decided There' table (process metadata — the live decisions are in the spine), the 'learning payoff is thin' strand of the debtor deferral rationale, PRD section ceremony
- (event) Spec finalized — SPEC.md + glossary.md + state-machines.md + roadmap.md, spine adopted; no open questions, 2 assumptions recorded
- (direction) Boss: rename the payment_intents table to payments
- (decision) Entity renamed payment_intents -> payments in glossary.md — provider-neutral, matches payment-service and the one-payment-per-ride rule; the provider's intent id stays a column, and AD-43 makes the provider swappable so its vocabulary should not name the table
- (note) Rename is spec-side only so far: ARCHITECTURE-SPINE.md (AD-3 ownership line, structural-seed ER diagram), architecture-map.html, and the PRD addendum data model still say payment_intents — spine is owned by bmad-architecture, not editable from here. Architecture review files are historical records of that run and stay as written
- (event) Update sweep PASS — single-term change, no capability, constraint or state-machine text affected; CAP ids untouched
- (note) PRD addendum data model updated to 'payments' too, so the absorbed source no longer contradicts the spec
- (note) Rename propagated into ARCHITECTURE-SPINE.md (AD-3 rule, ER diagram PAYMENT, prose) and architecture-map.html via bmad-architecture targeted amendment — AD ids unchanged. Spec, PRD addendum and spine now agree on 'payments'
- (direction) Boss caught a rendering error in the payment machine: CAPTURED --> FAILED is wrong, capture means the money moved
- (decision) SUPERSEDES the earlier payment-machine render: the exhausted-capture edge is AUTHORIZED --> FAILED, not CAPTURED --> FAILED. The payment stays AUTHORIZED for the whole capture attempt including retries, so CAPTURED is reached only on success and is left only by a deliberate refund. FR-37 and AD-50 name the cause but not the source state, so the error was mine at render time, not in the sources
- (question) OPEN: an exhausted capture (AUTHORIZED --> FAILED) leaves the hold neither captured nor voided while the ride is terminal COMPLETED — which reads against AD-44's stated invariant that a terminal ride never leaves an outstanding authorisation. Void it, or leave it to expire provider-side? Neither PRD nor spine says. Not invented here; surfaced to Boss
- (event) Update sweep PASS — CAP-27's wording never named a source state so it stays correct; SPEC.md and glossary untouched; AD-50's own text ('declined at authorisation, or capture retries exhausted') already agrees with the corrected edge, so no spine change is required for the fix itself
- (direction) Boss: keep retrying a failing capture instead of giving up at a cap; add a new terminal CAPTURE_FAILED for a hold the provider says is gone; add a metric for failed captures because that is lost money
- (decision) SUPERSEDES FR-37 as absorbed: capture is no longer abandoned on an exhausted retry budget. Retry persists across restarts while the payment stays AUTHORIZED; terminal only on provider-reported uncapturable hold. This is AD-45 applied to capture — the provider is external truth the system will eventually receive, so the wait is not bounded
- (decision) New terminal payment state CAPTURE_FAILED, separate from FAILED. Deliberate divergence from AD-18 (cause-as-field): FAILED is a declined authorization where nothing was lost, CAPTURE_FAILED is a delivered trip whose money is gone — different consequences, different consumers, only one is revenue loss
- (decision) Two money gauges added to CAP-36: capture loss (count + summed amount of CAPTURE_FAILED, zero in health) and age of the oldest capture still retrying — the second is the leading indicator since CAPTURE_FAILED is already unrecoverable when it fires
- (assumption) Assumed 'no longer capturable' means provider-reported expired/revoked/cancelled hold. Neither PRD nor spine defines the signal; the distinction that matters is provider-says-dead vs provider-unreachable, and only the first is terminal
- (note) CLOSES the earlier open question about the stranded hold after an exhausted capture — a hold now always ends captured, voided, or provably uncapturable, so AD-44's invariant holds without a carve-out
- (event) Update sweep PASS — CAP-27 rewritten, CAP-36 gained two gauges, payment machine and glossary Hold updated; CAP ids unchanged. Upstream now diverges: PRD FR-35/FR-37 and spine AD-50 still describe the retry-cap model
- (note) Missed on the first pass and now fixed: SPEC.md Non-goals still described the retry-cap model. Reworded — the non-goal is pursuit of UNRECOVERABLE money (CAPTURE_FAILED), never abandonment of a still-valid hold. Also scoped CAP-23's dead-letter-on-exhaustion so it cannot be read as applying to capture
- (question) OPEN for Boss: FR-7/CAP-5 rider-visible outcome. With unbounded retry a COMPLETED ride can sit with no settled payment outcome for a while. PRD run resolved it as 'rider sees an outcome only once the payment settles'; the alternative is an explicit rider-visible pending state. Not decided
- (note) flows.html (PRD run) is stale against the new payment model — its state SVG shows FAILED/VOIDED as the only terminals with an edge labelled 'capture exhausted'. Regeneration job, not a hand edit; left untouched
- (direction) Boss: after the hold confirms the ride no longer cares about payment; but a rider with a pending payment must be refused a new ride request
- (constraint) New admission rule: a rider whose previous ride is terminal while its payment is still INITIATED or AUTHORIZED cannot request a new ride. Rationale: unbounded capture retry (CAP-27) would otherwise let one rider stack unlimited unpaid trips during a provider outage
- (decision) CAPTURE_FAILED explicitly does NOT block a new request — a dead hold is money already lost, and refusing service over it is debtor standing, an explicit non-goal with no clearing mechanism. Guard covers money in flight only. Flagged to Boss for confirmation since it is the line between admission control and debtor standing
- (decision) The guard sits at request admission, not inside the ride lifecycle, so the ride machine stays free of payment after the hold confirms (AD-2, AD-41) and no path a rider waits on pays for it
- (note) This is the first payment -> ride coupling in the system; every other one runs ride -> payment. state-machines.md cross-coupling section corrected (it claimed 'two points, both one-directional' while listing three) and now carries both directions
- (question) OPEN for the spine: how matching-service learns a rider has an unsettled payment without violating database-per-service (AD-1/AD-3). Synchronous gRPC to payment-service puts a tier-2 dependency on the hot path of every ride request, which AD-41 worked to avoid; a local read-model projection fed by payment events keeps it local and fast but is eventually consistent, letting a rider slip one extra ride through the window. Recommend the projection — this is admission control, not a correctness invariant like double-booking
- (event) Update sweep PASS — constraint added, CAP-2 success extended, cross-coupling section rewritten; CAP ids unchanged; upstream PRD and spine both now behind on this rule
- (note) Spine pass reported two more spec stragglers, both fixed: the deferred-debtor bullet cited CAP-27's terminal FAILED (now CAPTURE_FAILED, and it now distinguishes the in-scope admission guard from out-of-scope debtor standing), and the capacity constraint's bare 'retry cap' now separates the outbox retry cap from the capture backoff ceiling (AD-58: bound on rate, not attempt count)
- (note) Spine now carries AD-58 (durable capture claim-loop worker in payment-service, backoff ceiling not cap, breaker before claim, three-way outcome classification) plus amendments to AD-50, AD-44, AD-54, AD-55, AD-43. AD-44's invariant now holds with no carve-out
- (question) OPEN, surfaced by the spine pass: nobody durably drives a VOID. AD-44 covers only a terminal event arriving before authorization resolves; the common case (rider cancels with the hold already AUTHORIZED) has no named owner, so a build could void in-process and lose it on restart. Pre-existing, visible only now that capture has a durable pursuit
- (question) OPEN, pre-existing: CAP-28's webhook signature verification has no AD, and the spine does not say whether webhook dedupe keys on the envelope event_id or the provider's event id
- (direction) Boss overrode the earlier call: CAPTURE_FAILED DOES block a new ride request, but only while recent — 30-minute window from the failure, then the rider is free
- (decision) SUPERSEDES the earlier 'CAPTURE_FAILED never blocks' decision. Cooldown of 30 min from capture_failed_at, counted from the rider's most recent one. The window itself is the clearing mechanism, which answers the objection that killed debtor standing (a one-way door with no way out) — no standing attached to the rider, no operator release
- (note) Mechanism correction recorded with the rule: an unreachable provider never yields CAPTURE_FAILED (that is just the next retry), so the state lands in a burst AFTER the provider returns and reports expired holds. The cooldown governs the aftermath, not the outage — the outage itself is already covered by the unsettled-payment arm
- (constraint) capture_failed_at is compared against wall clock, not a monotonic deadline — a stored fact read across restarts and services, same class as retention partitioning
- (constraint) 30 min is a new member of AD-46's tuned time set and must be ordered against it rather than chosen alone (it currently sits between IN_PROGRESS staleness at 10 min and session expiry at 1 h) — spine work
- (decision) CAPTURE_FAILED stays terminal for the MVP. Boss expects post-MVP reconciliation to recover some — recorded as deferred with the consequence named: leaving CAPTURE_FAILED is a state-machine change (AD-11 raises on illegal transitions), either a new transition or a fresh payment, never a quietly added background job
- (event) Update sweep PASS — two constraints replaced, deferred bullet extended, cross-coupling table and payment rules updated; CAP ids unchanged
- (note) PRD propagation done: new FR-51 carries both admission arms (not folded into FR-3, whose identity is concurrency rather than money outstanding); FR-2, FR-3, NFR-9, non-goals, deferred list, glossary and flows.html updated. Group A heading is now non-contiguous (FR-1-FR-8, FR-51) — accepted over renumbering
- (note) PRD run found and resolved a real contradiction the spec had already avoided: NFR-9 declared deadlines monotonic while the cooldown is wall clock. Resolved inside NFR-9 — the cooldown is a stored fact compared at admission across restarts, not an in-process deadline. Spec constraint already says the same, so the two agree
- (question) OPEN for Boss, raised by the PRD run: add a metric for FR-51 refusals? A burst of them is the visible signature of an outage aftermath, so it would fit CAP-36 next to the two money gauges. Not added anywhere yet — would need spec, PRD and spine together
- (direction) Boss approved the FR-51 refusal metric
- (decision) CAP-36 gains refused-ride-requests counted BY REASON (one active ride / unsettled payment / recent CAPTURE_FAILED). Split by reason is the point: one-active-ride refusals are ordinary rider behaviour, while a spike in either payment-driven arm is the signature of a provider failing or recovering, observed where a rider actually feels it
- (event) Update sweep PASS — CAP-36 extended, no other surface touched, CAP ids unchanged
- (note) Refusal metric landed in PRD FR-46 (beside the two money gauges) and NFR-5, worded as a prohibition on a single undifferentiated total; FR-51 cross-references it so caller-visible reasons and counted reasons read as one fact. flows.html enumerates no metrics, so only its FR-51 callout gained the rationale. Spine still pending
- (note) Spine pass added AD-59 (local payment_standing projection in matching-service, never a synchronous call), retitled AD-58 capture->settlement so one durable worker drives voids off void_requested_at, and amended AD-44, AD-46, AD-50, AD-48, AD-3, AD-14, AD-43, AD-54, AD-55. 30-min cooldown ordered as idle < MATCHED < IN_PROGRESS < cooldown < session expiry
- (question) OPEN, needs Boss to ratify: the spine narrowed admission arm 1. As approved (and as SPEC.md still says) a terminal ride with an INITIATED/AUTHORIZED payment blocks 'until it settles' — but under AD-58's uncapped pursuit that is unbounded, so a provider outage refuses every rider who completed a trip, indefinitely, which is the same Tier-1 breakage the rule cites for rejecting the synchronous call. Spine shipped: most recent ride COMPLETED with payment still AUTHORIZED, refused until it settles or the session-expiry bound lapses. Also excludes CANCELLED rides, since blocking there revokes AD-45's promise that the rider's own cancellation is their exit
- (note) SPEC.md and the spine currently DISAGREE on that arm — spec carries the broad approved wording, spine the narrowed one. Deliberately not reconciled until Boss rules
- (note) Still open from the spine pass: the FR-7 capability-map row routes payment-service -> rider-service, an edge the dependency graph does not contain; and architecture-map.html's topology SVG needs regenerating rather than patching (its aria-label already under-describes the existing Kafka fan-out)
- (direction) Boss ratified the spine's narrowing of admission arm 1
- (decision) SUPERSEDES the broader wording: arm 1 fires only when the rider's most recent ride is COMPLETED with its payment still AUTHORIZED, and is bounded — refused until it settles or the session-expiry bound lapses. CANCELLED rides awaiting a void never block, because that would revoke AD-45's promise that the rider's own cancellation is their exit. Bounded because capture pursuit is uncapped: an unbounded block is total stoppage wearing admission control's clothes, and past the bound the CAPTURE_FAILED cooldown does the throttling
- (event) Spec reconciled to the spine — SPEC.md constraints and CAP-2, state-machines.md coupling table and outage note. Spec and spine now agree on all three refusal grounds; CAP ids unchanged
- (constraint) All three refusal grounds read ONE anchor (the rider's most recent ride) and evaluate in fixed order, arm 1 before arm 2 — the gate found the spec and spine had three different anchors (most recent ride / payment / CAPTURE_FAILED), under which a rider could match two grounds or neither depending on the reading and refusal counts would stop summing to refusals
- (constraint) The refusal metric counts one increment per refused CALL, not per affected rider — a rider re-polling on the 2s driver-poll cadence contributes ~1800/hour, so the two readings differ by orders of magnitude and alerting is rate-based
- (note) Spine landed the metric in AD-54 with a pointer from AD-59, scoped to AD-38's 409 admission refusals only (AD-35's 503 shed and 400 malformed excluded, they have their own signals), made the no-collapsing rule enforceable on queries rather than the exporter, and gave each refusal a reason token surfaced as a distinct RFC 9457 type URI so the API distinguishes what the metric splits
- (note) Spine promoted metric derivation into a new Metrics row in Consistency Conventions: read every fact where it is authoritative — durable state from the owning table, process-local state from the process, events as in-process counters. Its first attempt used a gauge-vs-counter axis that would have forced a builder to invent a table for pool depth; the gate caught it
- (event) Update sweep PASS — anchor/order constraint added, CAP-36 counting unit fixed; spec and spine agree on all three grounds, their anchor, their order and their counting unit
- (direction) Boss approved tagging the capabilities by kind
- (decision) Every CAP heading now carries slice | property | enabler, with a legend under the Capabilities heading. 32 slices; properties CAP-13 CAP-20 CAP-23 CAP-24 CAP-31 (hold across stories, never one themselves); enablers CAP-36 CAP-39 CAP-40 (needed before the phase their subject sits in)
- (decision) roadmap.md gained a 'week one, before any phase' section for the three enablers, and states that properties are not scheduled at all — they attach as acceptance criteria to the stories that could violate them
- (assumption) Two classifications are arguable and were called rather than asked: CAP-31 (audit every transition) tagged property because the load-bearing part for story generation is the emission obligation on every earlier story, while the audit-service build lives in CAP-32/CAP-33; CAP-7 (authorization gates dispatch) tagged slice despite reading like an invariant, because it is delivered as one concrete flow
- (event) Update sweep PASS — 40/40 headings tagged, legend added, roadmap enabler section added; no capability text, ids or ordering changed
- (decision) Week 8 was an off-by-one, not a buffer — Boss confirmed. Phase 2 now runs weeks 8-16; every other boundary unchanged, plan still 32 weeks. Fixed in roadmap.md, prd.md section 6 and flows.html together
- (constraint) Boss: the week numbers are an ESTIMATE, not a commitment. roadmap.md now says so — they fix phase order and relative weight; what binds is the sequence, the milestone tags and the sequencing constraints. Sprint planning must not treat them as deadlines
