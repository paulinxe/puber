package com.puber.matching.rules.fixtures.entity;

/** Its package name is the whole violation: {@code entity} implies an ORM, and there is none. */
public final class AClassInAPackageNamedEntity {

    public String value() {
        return "value";
    }
}
